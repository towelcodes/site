---
const id = `${Math.random()}`;
---

<strong id={id}>00:00</strong> GMT
<script is:inline define:vars={{ id }}>
    const time = document.getElementById(id);
    time.textContent = new Date().toLocaleTimeString().substring(0, 5);
    setInterval(() => {
        time.textContent = new Date().toLocaleTimeString().substring(0, 5);
    }, 5000);
</script>
